# Avengers Assemble: Ported Hub Project Context

## Project State as of Last Session
- **Stack**: Next.js 15+ (App Router), Prisma, PostgreSQL (Neon), TypeScript.
- **Key Features Implemented**:
  - Full Admin Dashboard with Products, Users, and editable Revenue Analytics.
  - Custom "Fresh Drops" boolean logic to showcase new products on the homepage.
  - Image uploads bypass Vercel's read-only filesystem by using the GitHub API to commit images directly to `public/images/`.
  - Added automated GitHub API deletion logic when a product is deleted.
  - `package.json` build script upgraded to `npx prisma db push && prisma generate && next build` to keep Vercel's database perfectly synced.
- **User's Goal**: The user is transitioning into learning software development. They want to understand the backend architecture (Next.js route handlers, Prisma ORM, and API requests).

## Next Steps for the User
- Explore the backend API routes in `src/app/api/`.
- Understand how `prisma.product.findMany()` works.
- Play in the sandbox, break things, and read error messages to learn.

*When the user says "avengers assemble", reference this document to instantly resume context.*
